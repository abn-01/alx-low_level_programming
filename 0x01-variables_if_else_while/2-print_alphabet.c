#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Program for lowercase and uppercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char lowCase;

	for (lowCase = 'a'; lowCase <= 'z'; lowCase++)
		putchar(low);
	putchar('\n');

	return (0);
}
